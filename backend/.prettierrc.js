// .prettierrc.js

module.exports = {
  // Evita el uso de punto y coma al final de las declaraciones.
  semi: false,
  // Usa comillas simples en lugar de dobles.
  singleQuote: true,
  // Define el ancho máximo de la línea donde Prettier intentará ajustar el código.
  printWidth: 120,
  // Siempre incluye paréntesis alrededor de un único parámetro de función de flecha.
  arrowParens: "always",
  // Coloca el ">" de un tag HTML/JSX en la misma línea que el último atributo.
  jsxBracketSameLine: false,
  // Usa comas finales siempre que sea posible (para arrays y objetos).
  trailingComma: "all",
  // Usa tabulaciones en lugar de espacios.
  useTabs: false,
  // Especifica el número de espacios por nivel de sangría.
  tabWidth: 2,
};
